# O kit não tem package.json de propósito (zero build, zero dependência de
# runtime). Se alguém instalar algo para experimentar, não versionar.
node_modules/

# Saída de teste e captura de tela
*.png
!demo/**/*.png
test-results/
playwright-report/

# Sistema operacional / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# Sessão de agente: worktrees, transcrições e config local do Claude Code.
# São 5,7 MB de cópias do próprio repositório — nada disso é o kit.
.claude/
